library xmpstrings;

class XmpStrings {
  /// Convert the lower case strings to `upper case`
  String upperCase(String value) {
    return value.toUpperCase();
  }

  /// Convert the lower case strings to `lower case`
  String lowerCase(String value) {
    return value.toLowerCase();
  }
}
